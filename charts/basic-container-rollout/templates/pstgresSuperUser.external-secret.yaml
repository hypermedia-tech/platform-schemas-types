{{- if .Values.postgres.enabled }}
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: {{ .Values.postgres.dbname }}-db-su-secret
  namespace: {{ .Release.Namespace }}
spec:
  refreshInterval: {{ .Values.container.secretRefreshInterval | default "24h" }}
  secretStoreRef:
    name: {{ .Values.container.secretStore.name }}
    kind: ClusterSecretStore
  target:
    name: {{ .Values.postgres.dbname }}-db-su-secret
    creationPolicy: Owner
    template:
      type: kubernetes.io/basic-auth
  data:
    - secretKey: username
      remoteRef:
        key: secret/shared/{{ .Values.serviceName }}/postgres/su-credentials
        property: username
    - secretKey: password
      remoteRef:
        key: secret/shared/{{ .Values.serviceName }}/postgres/su-credentials
        property: password
{{- end }}
