apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: grafana-tls-cert
  namespace: monitoring
spec:
  secretName: grafana-tls-cert
  commonName: grafana.hypermedia.au
  dnsNames:
    - grafana.hypermedia.au
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
